# Supabase Migration 개발 계획

> **작성일**: 2026-01-15  
> **최종 수정**: 2026-01-15  
> **상태**: 🟡 Phase 1-2 준비 완료, Supabase 프로젝트 설정 대기 중  
> **목표**: TimeWalker 앱의 콘텐츠 데이터를 Supabase 백엔드로 마이그레이션

---

## 🎯 즉시 실행 가능한 다음 단계

1. **Supabase 프로젝트 생성**: https://supabase.com 에서 새 프로젝트 생성
2. **스키마 배포**: `tools/supabase/schema.sql` 실행
3. **데이터 마이그레이션**: `python tools/supabase/migrate_data.py` 실행
4. **앱 테스트**: 환경변수 설정 후 Flutter 앱 실행

### ✅ 이미 구현된 항목

| 구분 | 파일 | 구현 상태 | 비고 |
|------|------|----------|------|
| **설정** | `lib/core/config/supabase_config.dart` | ✅ 완료 | 환경변수 기반 설정 |
| **데이터 로더** | `lib/data/datasources/remote/supabase_content_loader.dart` | ✅ 완료 | 캐싱 + 버전 관리 |
| **레포지토리** | `supabase_character_repository.dart` | ✅ 완료 | Fallback 패턴 적용 |
| | `supabase_dialogue_repository.dart` | ✅ 완료 | |
| | `supabase_encyclopedia_repository.dart` | ✅ 완료 | |
| | `supabase_location_repository.dart` | ✅ 완료 | |
| | `supabase_quiz_repository.dart` | ✅ 완료 | |
| | `supabase_mapping_utils.dart` | ✅ 완료 | 공통 매핑 유틸 |
| **DB 스키마** | `tools/supabase/schema.sql` | ✅ 완료 | 6개 테이블 + RLS 정책 |
| **데이터 로드** | `tools/supabase/load.sql` | ✅ 완료 | JSON → 테이블 변환 |
| **의존성** | `supabase_flutter: ^2.6.0` | ✅ 완료 | pubspec.yaml에 추가 |
| **Provider 연동** | `repository_providers.dart` | ✅ 완료 | 조건부 Supabase/Mock 전환 |
| **앱 초기화** | `main.dart` | ✅ 완료 | Supabase.initialize 조건부 호출 |

### 🏗️ 현재 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         Flutter App                              │
├─────────────────────────────────────────────────────────────────┤
│  main.dart                                                       │
│  └─ Supabase.initialize() (if configured)                       │
├─────────────────────────────────────────────────────────────────┤
│  repository_providers.dart                                       │
│  └─ SupabaseConfig.isConfigured?                                │
│      ├─ true  → SupabaseXxxRepository                           │
│      └─ false → MockXxxRepository (로컬 JSON 사용)             │
├─────────────────────────────────────────────────────────────────┤
│  SupabaseXxxRepository                                           │
│  └─ SupabaseContentLoader                                        │
│      ├─ 1. content_versions 테이블에서 버전 확인                │
│      ├─ 2. 버전 다르면 → Supabase에서 fetch + Hive 캐시 저장    │
│      ├─ 3. 버전 같으면 → Hive 캐시에서 로드                     │
│      └─ 4. 실패 시 → 로컬 JSON 파일 fallback                    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         Supabase                                 │
├─────────────────────────────────────────────────────────────────┤
│  Tables:                                                         │
│  ├─ characters          (캐릭터 정보)                           │
│  ├─ dialogues           (대화 스크립트)                         │
│  ├─ locations           (장소 정보)                             │
│  ├─ encyclopedia_entries (백과사전 항목)                        │
│  ├─ quiz_categories     (퀴즈 카테고리)                         │
│  ├─ quizzes             (퀴즈 문제)                             │
│  └─ content_versions    (버전 관리)                             │
├─────────────────────────────────────────────────────────────────┤
│  RLS Policies:                                                   │
│  ├─ Public Read (anon key로 읽기 가능)                          │
│  └─ Service Role Write (서버에서만 쓰기 가능)                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🚀 개발 계획 (4단계)

### Phase 1: Supabase 프로젝트 설정 및 스키마 배포 (1일)

**목표**: Supabase 프로젝트 생성 및 데이터베이스 스키마 적용

| Task | 상세 내용 | 담당 |
|------|----------|------|
| 1.1 | Supabase 프로젝트 생성 (supabase.com) | 수동 |
| 1.2 | `tools/supabase/schema.sql` 실행하여 테이블 생성 | SQL Editor |
| 1.3 | RLS 정책 확인 및 테스트 | Dashboard |
| 1.4 | API URL 및 anon key 확보 | Settings > API |

**체크리스트**:
- [ ] Supabase 프로젝트 생성 완료
- [ ] 모든 테이블 생성 확인 (characters, dialogues, locations, encyclopedia_entries, quiz_categories, quizzes, content_versions)
- [ ] 인덱스 생성 확인
- [ ] RLS 정책 적용 확인
- [ ] API 키 안전하게 보관

---

### Phase 2: 데이터 마이그레이션 (2일)

**목표**: 로컬 JSON 데이터를 Supabase로 업로드

| Task | 상세 내용 | 우선순위 |
|------|----------|----------|
| 2.1 | JSON → Staging 테이블 변환 스크립트 작성 | 높음 |
| 2.2 | `load.sql` 실행하여 데이터 로드 | 높음 |
| 2.3 | 데이터 무결성 검증 | 높음 |
| 2.4 | content_versions 초기값 설정 | 중간 |

**데이터 소스 파일**:
```
assets/data/
├── characters.json      → characters 테이블
├── dialogues.json       → dialogues 테이블
├── locations.json       → locations 테이블
├── encyclopedia.json    → encyclopedia_entries 테이블
└── quizzes.json         → quiz_categories + quizzes 테이블
```

**마이그레이션 스크립트 (추가 개발 필요)**:
```bash
# tools/supabase/migrate_data.py (신규 생성)
# JSON 파일을 읽어서 Supabase staging 테이블에 삽입
```

**체크리스트**:
- [ ] 마이그레이션 스크립트 작성
- [ ] characters 데이터 업로드 (현재 약 50+ 캐릭터)
- [ ] dialogues 데이터 업로드
- [ ] locations 데이터 업로드 (현재 약 60+ 장소)
- [ ] encyclopedia_entries 데이터 업로드 (현재 약 100+ 항목)
- [ ] quizzes 데이터 업로드 (현재 약 150+ 퀴즈)
- [ ] 데이터 검증 쿼리 실행

---

### Phase 3: 앱 연동 테스트 (2일)

**목표**: 실제 앱에서 Supabase 데이터 로드 테스트

| Task | 상세 내용 | 우선순위 |
|------|----------|----------|
| 3.1 | 환경변수 설정 방법 문서화 | 높음 |
| 3.2 | 개발 환경에서 Supabase 연동 테스트 | 높음 |
| 3.3 | 오프라인 모드 (캐싱) 테스트 | 높음 |
| 3.4 | Fallback 시나리오 테스트 | 중간 |
| 3.5 | 성능 측정 (로딩 시간, 캐시 효과) | 중간 |

**환경변수 설정 방법**:
```bash
# VS Code launch.json / Android Studio
flutter run --dart-define=SUPABASE_URL=https://xxx.supabase.co \
            --dart-define=SUPABASE_ANON_KEY=eyJxxx...

# 또는 .env 파일 사용 (flutter_dotenv 패키지 추가 필요)
```

**테스트 시나리오**:
1. **정상 케이스**: Supabase 연결 → 데이터 로드 성공
2. **캐시 케이스**: 동일 버전 → Hive 캐시에서 로드
3. **오프라인 케이스**: 네트워크 끊김 → Hive 캐시 사용
4. **Fallback 케이스**: Supabase 미설정 → 로컬 JSON 사용

**체크리스트**:
- [ ] iOS 시뮬레이터에서 Supabase 연동 테스트
- [ ] Android 에뮬레이터에서 Supabase 연동 테스트
- [ ] 네트워크 끊김 시 캐시 동작 확인
- [ ] Mock 모드 (환경변수 미설정) 동작 확인
- [ ] 성능 리포트 작성

---

### Phase 4: 프로덕션 배포 준비 (2일)

**목표**: 프로덕션 환경 설정 및 CI/CD 구성

| Task | 상세 내용 | 우선순위 |
|------|----------|----------|
| 4.1 | 프로덕션 Supabase 프로젝트 설정 | 높음 |
| 4.2 | 환경별 설정 분리 (dev/staging/prod) | 높음 |
| 4.3 | CI/CD에 환경변수 설정 추가 | 높음 |
| 4.4 | 모니터링 및 알림 설정 | 중간 |
| 4.5 | 데이터 백업 정책 수립 | 중간 |

**환경 분리 구조**:
```
.env.development     → 개발용 Supabase 프로젝트
.env.staging         → 스테이징 Supabase 프로젝트
.env.production      → 프로덕션 Supabase 프로젝트
```

**체크리스트**:
- [ ] 프로덕션 Supabase 프로젝트 생성
- [ ] 프로덕션 데이터 마이그레이션
- [ ] GitHub Actions 시크릿 설정
- [ ] 앱 스토어 배포 테스트
- [ ] 모니터링 대시보드 설정

---

## 📋 추가 개발 필요 항목

### 1. 마이그레이션 스크립트 (신규)
```
tools/supabase/
├── migrate_data.py         # JSON → Supabase 마이그레이션 (신규)
├── validate_data.py        # 데이터 검증 스크립트 (신규)
└── update_version.py       # content_versions 업데이트 (신규)
```

### 2. 환경변수 관리 개선 (선택)
```dart
// flutter_dotenv 패키지 추가 시
// lib/core/config/supabase_config.dart 수정

import 'package:flutter_dotenv/flutter_dotenv.dart';

class SupabaseConfig {
  static String get url => 
      const String.fromEnvironment('SUPABASE_URL', defaultValue: '') 
      .isNotEmpty 
      ? const String.fromEnvironment('SUPABASE_URL') 
      : dotenv.get('SUPABASE_URL', fallback: '');
  // ...
}
```

### 3. 사용자 데이터 동기화 (향후)
현재는 콘텐츠 데이터만 Supabase에 저장하지만, 향후 사용자 진행 상태도 동기화할 수 있음:
- Supabase Auth 연동
- user_progress 테이블 추가
- 기기 간 동기화 기능

---

## ⏱️ 예상 일정

| Phase | 작업 내용 | 예상 기간 | 종료 예정일 |
|-------|----------|----------|------------|
| Phase 1 | Supabase 설정 | 1일 | 2026-01-16 |
| Phase 2 | 데이터 마이그레이션 | 2일 | 2026-01-18 |
| Phase 3 | 앱 연동 테스트 | 2일 | 2026-01-20 |
| Phase 4 | 프로덕션 배포 | 2일 | 2026-01-22 |
| **총계** | | **7일** | |

---

## 🔧 즉시 실행 가능한 명령어

### Supabase 스키마 배포 후 데이터 로드
```sql
-- Supabase SQL Editor에서 실행
\i tools/supabase/schema.sql
\i tools/supabase/load.sql
```

### Flutter 앱 Supabase 연동 테스트
```bash
flutter run --dart-define=SUPABASE_URL=https://your-project.supabase.co \
            --dart-define=SUPABASE_ANON_KEY=your-anon-key
```

### 환경변수 없이 Mock 모드로 실행
```bash
flutter run  # 기존처럼 로컬 JSON 사용
```

---

## 📌 주의사항

1. **API 키 보안**: anon key는 클라이언트에 노출되어도 안전하지만, service role key는 절대 노출 금지
2. **RLS 정책**: 모든 테이블에 읽기 전용 정책이 적용되어 있어 클라이언트에서 직접 수정 불가
3. **Fallback 동작**: Supabase 연결 실패 시 로컬 JSON 파일 사용 (앱 패키지에 포함)
4. **캐시 관리**: 버전이 변경되면 자동으로 새 데이터 다운로드
5. **이미지 에셋**: 현재 이미지는 앱 내 에셋으로 유지 (Supabase Storage 마이그레이션은 별도 계획)

---

## 📚 참고 문서

- [Supabase Flutter 공식 문서](https://supabase.com/docs/reference/dart/introduction)
- [Supabase RLS 가이드](https://supabase.com/docs/guides/auth/row-level-security)
- [Flutter 환경변수 설정](https://dart.dev/guides/environment-declarations)
